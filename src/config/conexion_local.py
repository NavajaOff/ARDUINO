config_mysql_local = {
    'user': 'root',  # Usuario por defecto de MySQL
    'password': '',  # Contraseña vacía por defecto (o la que tenga configurada)
    'host': 'localhost',
    'database': 'arduino_peaje',
    'raise_on_warnings': True,
    'auth_plugin': 'mysql_native_password'
}
